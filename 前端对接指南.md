# 微信记账小程序 - 前端对接指南

## 一、接口总体说明

### 1.1 接口规范

- **请求方式**：所有接口统一使用 `POST` 方法
- **数据格式**：统一使用 `JSON` 格式
- **请求头**：
  ```
  Content-Type: application/json
  Authorization: Bearer {token}  // 除登录接口外，其他接口都需要携带Token
  ```

### 1.2 统一请求格式

所有接口的请求参数都需要包装在 `UnifiedRequest` 对象中：

```json
{
  "data": {
    // 具体业务参数
  },
  "timestamp": 1699999999999,  // 可选，时间戳
  "sign": "xxx"  // 可选，签名
}
```

### 1.3 统一响应格式

所有接口的响应数据都遵循以下格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    // 响应数据
  },
  "timestamp": "2026-05-22T10:30:00"
}
```

#### 响应状态码说明：
| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权/Token无效 |
| 403 | 无权限访问 |
| 500 | 服务器内部错误 |

### 1.4 分页响应格式

分页查询接口的 `data` 字段为分页对象：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [
      // 数据列表
    ],
    "total": 100,
    "size": 10,
    "current": 1,
    "pages": 10,
    "hasPrevious": false,
    "hasNext": true
  }
}
```

---

## 二、接口详细说明

---

### 模块一：用户模块（User）

#### 1. 微信登录

**请求URL**：`POST /user/login`

**接口说明**：用户使用微信授权码登录系统，自动创建用户或返回已存在用户信息。

**请求参数**：
```json
{
  "data": {
    "code": "微信授权code",
    "deviceType": 1,
    "deviceId": "设备唯一标识"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| code | String | ✅ | 微信登录授权码（通过wx.login获取） |
| deviceType | Integer | ❌ | 设备类型：1-小程序，2-Android，3-iOS，默认1 |
| deviceId | String | ❌ | 设备唯一标识 |

**响应示例**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "expiresIn": 604800,
    "userInfo": {
      "id": 1234567890,
      "nickname": "张三",
      "avatarUrl": "https://example.com/avatar.jpg",
      "phone": "13800138000",
      "openId": "oXXXXX",
      "status": 1,
      "role": 0,
      "createTime": "2026-05-01T10:00:00"
    }
  }
}
```

**响应字段说明**：
| 字段 | 类型 | 说明 |
|------|------|------|
| token | String | JWT访问令牌 |
| expiresIn | Long | Token过期时间（秒），默认7天 |
| userInfo | Object | 用户信息对象 |
| userInfo.id | Long | 用户ID |
| userInfo.nickname | String | 用户昵称 |
| userInfo.avatarUrl | String | 头像URL |
| userInfo.phone | String | 手机号（可为空） |
| userInfo.openId | String | 微信OpenID |
| userInfo.status | Integer | 状态：1-正常 |
| userInfo.role | Integer | 角色：0-普通用户，1-管理员 |
| userInfo.createTime | String | 注册时间 |

---

#### 2. 获取当前用户信息

**请求URL**：`POST /user/info`

**接口说明**：获取当前登录用户的详细信息。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {}
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1234567890,
    "nickname": "张三",
    "avatarUrl": "https://example.com/avatar.jpg",
    "phone": "13800138000",
    "openId": "oXXXXX",
    "status": 1,
    "role": 0,
    "createTime": "2026-05-01T10:00:00"
  }
}
```

---

#### 3. 更新用户信息

**请求URL**：`POST /user/update`

**接口说明**：更新当前登录用户的个人信息（昵称、头像、手机号）。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "nickname": "新昵称",
    "avatarUrl": "https://example.com/new-avatar.jpg",
    "phone": "13900139000"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| nickname | String | ❌ | 新昵称 |
| avatarUrl | String | ❌ | 新头像URL |
| phone | String | ❌ | 新手机号 |

**响应示例**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 1234567890,
    "nickname": "新昵称",
    "avatarUrl": "https://example.com/new-avatar.jpg",
    "phone": "13900139000",
    "openId": "oXXXXX",
    "status": 1,
    "role": 0,
    "createTime": "2026-05-01T10:00:00"
  }
}
```

---

#### 4. 用户登出

**请求URL**：`POST /user/logout`

**接口说明**：用户退出登录，清除本地Token。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {}
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "登出成功",
  "data": null
}
```

---

## 三、账单模块（Record）

#### 1. 创建账单

**请求URL**：`POST /record`

**接口说明**：创建一条新的账单记录。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "categoryId": 1234567890,
    "type": 1,
    "amount": 100.50,
    "contactId": 9876543210,
    "contactName": "张三",
    "recordDate": "2026-05-22",
    "remark": "购买生活用品"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| categoryId | Long | ✅ | 类别ID |
| type | Integer | ✅ | 类型：1-支出，2-收入 |
| amount | BigDecimal | ✅ | 金额 |
| contactId | Long | ❌ | 联系人ID |
| contactName | String | ❌ | 联系人姓名 |
| recordDate | String | ❌ | 记账日期，默认当天 |
| remark | String | ❌ | 备注 |

**响应示例**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 111222333,
    "categoryId": 1234567890,
    "categoryName": "餐饮",
    "categoryIcon": "🍜",
    "type": 1,
    "amount": 100.50,
    "contactId": 9876543210,
    "contactName": "张三",
    "recordDate": "2026-05-22",
    "remark": "购买生活用品",
    "createTime": "2026-05-22T10:30:00",
    "updateTime": "2026-05-22T10:30:00"
  }
}
```

---

#### 2. 更新账单

**请求URL**：`POST /record/update`

**接口说明**：更新指定的账单记录。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "id": 111222333,
    "categoryId": 1234567890,
    "type": 1,
    "amount": 150.00,
    "contactId": 9876543210,
    "contactName": "张三",
    "recordDate": "2026-05-22",
    "remark": "更新备注"
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 111222333,
    "categoryId": 1234567890,
    "categoryName": "餐饮",
    "categoryIcon": "🍜",
    "type": 1,
    "amount": 150.00,
    "contactId": 9876543210,
    "contactName": "张三",
    "recordDate": "2026-05-22",
    "remark": "更新备注",
    "createTime": "2026-05-22T10:30:00",
    "updateTime": "2026-05-22T11:00:00"
  }
}
```

---

#### 3. 删除账单

**请求URL**：`POST /record/delete`

**接口说明**：删除指定的账单记录。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "id": 111222333
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | Long | ✅ | 账单ID |

**响应示例**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 4. 获取账单详情

**请求URL**：`POST /record/detail`

**接口说明**：获取指定账单的详细信息。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "id": 111222333
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 111222333,
    "categoryId": 1234567890,
    "categoryName": "餐饮",
    "categoryIcon": "🍜",
    "type": 1,
    "amount": 150.00,
    "contactId": 9876543210,
    "contactName": "张三",
    "recordDate": "2026-05-22",
    "remark": "更新备注",
    "createTime": "2026-05-22T10:30:00",
    "updateTime": "2026-05-22T11:00:00"
  }
}
```

---

#### 5. 获取账单列表

**请求URL**：`POST /record/list`

**接口说明**：分页查询账单记录列表。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "type": 1,
    "categoryId": 1234567890,
    "contactName": "张三",
    "startDate": "2026-05-01",
    "endDate": "2026-05-31",
    "current": 1,
    "size": 10
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | Integer | ❌ | 类型：1-支出，2-收入 |
| categoryId | Long | ❌ | 类别ID |
| contactName | String | ❌ | 联系人姓名（模糊查询） |
| startDate | String | ❌ | 开始日期（yyyy-MM-dd） |
| endDate | String | ❌ | 结束日期（yyyy-MM-dd） |
| current | Long | ❌ | 当前页码，默认1 |
| size | Long | ❌ | 每页条数，默认10 |

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 111222333,
      "categoryId": 1234567890,
      "categoryName": "餐饮",
      "categoryIcon": "🍜",
      "type": 1,
      "amount": 150.00,
      "contactId": 9876543210,
      "contactName": "张三",
      "recordDate": "2026-05-22",
      "remark": "午餐",
      "createTime": "2026-05-22T10:30:00",
      "updateTime": "2026-05-22T11:00:00"
    },
    {
      "id": 111222334,
      "categoryId": 1234567891,
      "categoryName": "交通",
      "categoryIcon": "🚌",
      "type": 1,
      "amount": 5.00,
      "contactId": null,
      "contactName": null,
      "recordDate": "2026-05-22",
      "remark": "地铁",
      "createTime": "2026-05-22T09:00:00",
      "updateTime": "2026-05-22T09:00:00"
    }
  ]
}
```

---

#### 6. 获取账单统计

**请求URL**：`POST /record/statistics`

**接口说明**：获取指定时间段的收支统计数据。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "startDate": "2026-05-01",
    "endDate": "2026-05-31"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startDate | String | ❌ | 开始日期，默认当月第一天 |
| endDate | String | ❌ | 结束日期，默认当天 |

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "totalIncome": 5000.00,
    "totalExpense": 3500.00,
    "balance": 1500.00,
    "incomeCount": 2,
    "expenseCount": 15
  }
}
```

**响应字段说明**：
| 字段 | 类型 | 说明 |
|------|------|------|
| totalIncome | BigDecimal | 总收入 |
| totalExpense | BigDecimal | 总支出 |
| balance | BigDecimal | 结余（收入-支出） |
| incomeCount | Integer | 收入笔数 |
| expenseCount | Integer | 支出笔数 |

---

## 四、类别模块（Category）

#### 1. 获取类别列表

**请求URL**：`POST /category/list`

**接口说明**：获取当前用户的账单类别列表，包含系统默认类别和用户自定义类别。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "type": 1
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | Integer | ❌ | 类型：1-支出，2-收入，不传则返回全部 |

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1,
      "name": "餐饮",
      "type": 1,
      "icon": "🍜",
      "sort": 1,
      "createTime": "2026-01-01T00:00:00"
    },
    {
      "id": 2,
      "name": "交通",
      "type": 1,
      "icon": "🚌",
      "sort": 2,
      "createTime": "2026-01-01T00:00:00"
    },
    {
      "id": 3,
      "name": "工资",
      "type": 2,
      "icon": "💰",
      "sort": 1,
      "createTime": "2026-01-01T00:00:00"
    }
  ]
}
```

---

#### 2. 创建类别

**请求URL**：`POST /category/create`

**接口说明**：创建一个新的账单类别。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "name": "娱乐",
    "type": 1,
    "icon": "🎮"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | String | ✅ | 类别名称 |
| type | Integer | ✅ | 类型：1-支出，2-收入 |
| icon | String | ❌ | 图标（emoji或图标名称） |

**响应示例**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 100,
    "name": "娱乐",
    "type": 1,
    "icon": "🎮",
    "sort": 10,
    "createTime": "2026-05-22T12:00:00"
  }
}
```

---

#### 3. 更新类别

**请求URL**：`POST /category/update`

**接口说明**：更新指定的账单类别。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "id": 100,
    "name": "游戏娱乐",
    "icon": "🎲"
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 100,
    "name": "游戏娱乐",
    "type": 1,
    "icon": "🎲",
    "sort": 10,
    "createTime": "2026-05-22T12:00:00"
  }
}
```

---

#### 4. 删除类别

**请求URL**：`POST /category/delete`

**接口说明**：删除指定的账单类别（仅支持删除用户自定义类别）。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "id": 100
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

## 五、联系人模块（Contact）

#### 1. 创建联系人

**请求URL**：`POST /contact/create`

**接口说明**：创建一个新的联系人。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "name": "李四",
    "phone": "13900139000",
    "openId": "oXXXXX",
    "relation": "friend",
    "remark": "大学同学"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | String | ✅ | 联系人姓名 |
| phone | String | ❌ | 联系电话 |
| openId | String | ❌ | 微信OpenID |
| relation | String | ❌ | 关系类型：friend/family/colleague/other |
| remark | String | ❌ | 备注 |

**响应示例**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 200,
    "name": "李四",
    "phone": "13900139000",
    "openId": "oXXXXX",
    "relation": "friend",
    "remark": "大学同学",
    "createTime": "2026-05-22T14:00:00"
  }
}
```

---

#### 2. 更新联系人

**请求URL**：`POST /contact/update`

**请求参数**：
```json
{
  "data": {
    "id": 200,
    "name": "李四（新）",
    "phone": "13800138000",
    "relation": "family"
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 200,
    "name": "李四（新）",
    "phone": "13800138000",
    "openId": "oXXXXX",
    "relation": "family",
    "remark": "大学同学",
    "createTime": "2026-05-22T14:00:00"
  }
}
```

---

#### 3. 删除联系人

**请求URL**：`POST /contact/delete`

**请求参数**：
```json
{
  "data": {
    "id": 200
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 4. 获取联系人详情

**请求URL**：`POST /contact/detail`

**请求参数**：
```json
{
  "data": {
    "id": 200
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 200,
    "name": "李四",
    "phone": "13900139000",
    "openId": "oXXXXX",
    "relation": "friend",
    "remark": "大学同学",
    "createTime": "2026-05-22T14:00:00"
  }
}
```

---

#### 5. 获取联系人列表

**请求URL**：`POST /contact/list`

**接口说明**：分页查询联系人列表。

**请求参数**：
```json
{
  "data": {
    "keyword": "李",
    "current": 1,
    "size": 10
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| keyword | String | ❌ | 搜索关键词（匹配姓名、电话、关系） |
| current | Long | ❌ | 当前页码，默认1 |
| size | Long | ❌ | 每页条数，默认10 |

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [
      {
        "id": 200,
        "name": "李四",
        "phone": "13900139000",
        "openId": "oXXXXX",
        "relation": "friend",
        "remark": "大学同学",
        "createTime": "2026-05-22T14:00:00"
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1,
    "hasPrevious": false,
    "hasNext": false
  }
}
```

---

## 六、我的事件模块（MyEvent）

#### 1. 创建事件

**请求URL**：`POST /my-event/create`

**接口说明**：创建一个新的事件（如婚礼、生日、乔迁等），用于向好友发送提醒通知。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "title": "我的婚礼",
    "eventType": 1,
    "eventDate": "2026-06-01",
    "location": "北京市朝阳区某某酒店",
    "remark": "诚邀各位亲朋好友参加",
    "advanceDays": 3
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| title | String | ✅ | 事件标题 |
| eventType | Integer | ✅ | 事件类型：1-结婚、2-满月酒、3-生日、4-升学、5-乔迁、6-其他 |
| eventDate | String | ✅ | 事件日期（yyyy-MM-dd） |
| location | String | ❌ | 事件地点 |
| remark | String | ❌ | 备注说明 |
| advanceDays | Integer | ❌ | 提前提醒天数，默认3天 |

**响应示例**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 300,
    "userId": 1234567890,
    "title": "我的婚礼",
    "eventType": 1,
    "eventDate": "2026-06-01",
    "location": "北京市朝阳区某某酒店",
    "remark": "诚邀各位亲朋好友参加",
    "advanceDays": 3,
    "status": 0,
    "createTime": "2026-05-22T15:00:00"
  }
}
```

**响应字段说明**：
| 字段 | 类型 | 说明 |
|------|------|------|
| status | Integer | 状态：0-未推送、1-已推送 |

---

#### 2. 更新事件

**请求URL**：`POST /my-event/update`

**请求参数**：
```json
{
  "data": {
    "id": 300,
    "title": "我的婚礼（更新）",
    "location": "北京市海淀区新酒店"
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 300,
    "userId": 1234567890,
    "title": "我的婚礼（更新）",
    "eventType": 1,
    "eventDate": "2026-06-01",
    "location": "北京市海淀区新酒店",
    "remark": "诚邀各位亲朋好友参加",
    "advanceDays": 3,
    "status": 0,
    "createTime": "2026-05-22T15:00:00"
  }
}
```

---

#### 3. 删除事件

**请求URL**：`POST /my-event/delete`

**请求参数**：
```json
{
  "data": {
    "id": 300
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 4. 获取事件详情

**请求URL**：`POST /my-event/detail`

**请求参数**：
```json
{
  "data": {
    "id": 300
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 300,
    "userId": 1234567890,
    "title": "我的婚礼",
    "eventType": 1,
    "eventDate": "2026-06-01",
    "location": "北京市朝阳区某某酒店",
    "remark": "诚邀各位亲朋好友参加",
    "advanceDays": 3,
    "status": 0,
    "createTime": "2026-05-22T15:00:00"
  }
}
```

---

#### 5. 获取事件列表

**请求URL**：`POST /my-event/list`

**接口说明**：分页查询我的事件列表。

**请求参数**：
```json
{
  "data": {
    "status": 0,
    "current": 1,
    "size": 10
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| status | Integer | ❌ | 状态：0-待推送、1-已推送，不传返回全部 |
| current | Long | ❌ | 当前页码，默认1 |
| size | Long | ❌ | 每页条数，默认10 |

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [
      {
        "id": 300,
        "userId": 1234567890,
        "title": "我的婚礼",
        "eventType": 1,
        "eventDate": "2026-06-01",
        "location": "北京市朝阳区某某酒店",
        "remark": "诚邀各位亲朋好友参加",
        "advanceDays": 3,
        "status": 0,
        "createTime": "2026-05-22T15:00:00"
      }
    ],
    "total": 1,
    "size": 10,
    "current": 1,
    "pages": 1,
    "hasPrevious": false,
    "hasNext": false
  }
}
```

---

## 七、分享模块（Share）

#### 1. 创建分享邀请

**请求URL**：`POST /share/create`

**接口说明**：创建分享链接，可以是事件分享或好友邀请。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "shareType": 1,
    "relatedId": 300,
    "relation": "friend"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| shareType | Integer | ✅ | 分享类型：1-事件分享、2-账单分享、3-邀请注册 |
| relatedId | Long | ❌ | 关联ID（事件ID或账单ID） |
| relation | String | ❌ | 关系类型：friend/family/colleague/other |

**响应示例**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 400,
    "shareCode": "abc123def456",
    "shareUrl": "/pages/index/index?shareCode=abc123def456",
    "shareType": 1,
    "status": 0,
    "expireTime": "2026-05-29T15:00:00",
    "shareNickname": "张三",
    "shareAvatar": "https://example.com/avatar.jpg",
    "event": {
      "id": 300,
      "title": "我的婚礼",
      "eventType": 1,
      "eventDate": "2026-06-01",
      "location": "北京市朝阳区某某酒店"
    },
    "createTime": "2026-05-22T15:00:00"
  }
}
```

---

#### 2. 获取分享信息

**请求URL**：`POST /share/info`

**接口说明**：通过分享码获取分享信息（无需登录）。

**请求参数**：
```json
{
  "data": {
    "shareCode": "abc123def456"
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 400,
    "shareCode": "abc123def456",
    "shareUrl": "/pages/index/index?shareCode=abc123def456",
    "shareType": 1,
    "status": 0,
    "expireTime": "2026-05-29T15:00:00",
    "shareNickname": "张三",
    "shareAvatar": "https://example.com/avatar.jpg",
    "event": {
      "id": 300,
      "title": "我的婚礼",
      "eventType": 1,
      "eventDate": "2026-06-01",
      "location": "北京市朝阳区某某酒店"
    },
    "createTime": "2026-05-22T15:00:00"
  }
}
```

---

#### 3. 接受分享邀请

**请求URL**：`POST /share/accept`

**接口说明**：接受分享邀请，建立好友关系（需要登录）。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "shareCode": "abc123def456"
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "接受成功",
  "data": null
}
```

---

## 八、提醒任务模块（Reminder）

#### 1. 创建提醒任务

**请求URL**：`POST /reminder/create`

**接口说明**：创建一个提醒任务。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "title": "婚礼提醒",
    "remindType": 1,
    "remindDate": "2026-05-30",
    "content": "张三的婚礼将于6月1日举行",
    "contactIds": [200, 201, 202]
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| title | String | ✅ | 提醒标题 |
| remindType | Integer | ✅ | 提醒类型：1-婚宴，2-生日，3-乔迁，4-其他 |
| remindDate | String | ✅ | 提醒日期（yyyy-MM-dd） |
| content | String | ❌ | 提醒内容 |
| contactIds | List | ❌ | 关联的联系人ID列表 |

**响应示例**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 500,
    "userId": 1234567890,
    "title": "婚礼提醒",
    "remindType": 1,
    "remindDate": "2026-05-30",
    "content": "张三的婚礼将于6月1日举行",
    "status": 0,
    "contacts": [
      {
        "id": 200,
        "name": "李四",
        "phone": "13900139000"
      }
    ],
    "createTime": "2026-05-22T16:00:00"
  }
}
```

---

#### 2. 更新提醒任务

**请求URL**：`POST /reminder/update`

**请求参数**：
```json
{
  "data": {
    "id": 500,
    "title": "婚礼提醒（更新）",
    "contactIds": [200, 201]
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 500,
    "userId": 1234567890,
    "title": "婚礼提醒（更新）",
    "remindType": 1,
    "remindDate": "2026-05-30",
    "content": "张三的婚礼将于6月1日举行",
    "status": 0,
    "contacts": [...],
    "createTime": "2026-05-22T16:00:00"
  }
}
```

---

#### 3. 删除提醒任务

**请求URL**：`POST /reminder/delete`

**请求参数**：
```json
{
  "data": {
    "id": 500
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 4. 获取提醒任务详情

**请求URL**：`POST /reminder/detail`

**请求参数**：
```json
{
  "data": {
    "id": 500
  }
}
```

---

#### 5. 获取提醒任务列表

**请求URL**：`POST /reminder/list`

**请求参数**：
```json
{
  "data": {
    "status": 0,
    "current": 1,
    "size": 10
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| status | Integer | ❌ | 状态：0-待发送、1-已发送、2-已取消 |
| current | Long | ❌ | 当前页码，默认1 |
| size | Long | ❌ | 每页条数，默认10 |

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [...],
    "total": 5,
    "size": 10,
    "current": 1,
    "pages": 1,
    "hasPrevious": false,
    "hasNext": false
  }
}
```

---

#### 6. 取消提醒任务

**请求URL**：`POST /reminder/cancel`

**请求参数**：
```json
{
  "data": {
    "id": 500
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "取消成功",
  "data": null
}
```

---

#### 7. 生成分享参数

**请求URL**：`POST /reminder/share-params`

**接口说明**：生成提醒任务的分享参数。

**请求参数**：
```json
{
  "data": {
    "taskId": 500,
    "contactId": 200
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "shareParams": "shareId=xxx&taskId=500&contactId=200&userName=张三"
  }
}
```

---

## 九、接口调用顺序

### 9.1 用户登录流程

```
1. POST /user/login
   ↓
   获取 token 和 userInfo
   ↓
   保存 token 到本地存储
   ↓
2. POST /user/info（获取完整用户信息，可选）
```

### 9.2 首页数据加载流程

```
1. POST /category/list（获取账单类别）
   ↓
2. POST /record/statistics（获取本月统计）
   ↓
3. POST /record/list（获取最近账单）
   ↓
4. POST /my-event/list（获取即将到来的事件）
```

### 9.3 添加账单流程

```
1. POST /category/list（选择类别）
   ↓
2. POST /contact/list（选择联系人，可选）
   ↓
3. POST /record（创建账单）
```

### 9.4 创建事件并分享流程

```
1. POST /my-event/create（创建事件）
   ↓
2. POST /share/create（创建分享）
   ↓
3. 分享 shareCode 给好友
```

### 9.5 好友接受分享流程

```
1. 好友打开分享链接
   ↓
2. POST /share/info（获取分享信息）
   ↓
3. POST /user/login（如果未登录）
   ↓
4. POST /share/accept（接受分享，建立好友关系）
```

---

## 十、错误码说明

| 错误信息 | 可能原因 | 解决方案 |
|----------|----------|----------|
| 类别不存在 | categoryId 错误 | 检查 categoryId 是否正确 |
| 类别类型不匹配 | 支出类别不能用于收入 | 确保 type 与 categoryId 对应 |
| 账单不存在 | id 错误或已被删除 | 检查 id 是否正确 |
| 无权修改该账单 | 非账单所有者 | 确保是账单创建者 |
| 联系人不存在 | id 错误或已被删除 | 检查 id 是否正确 |
| 分享链接不存在 | shareCode 错误或已过期 | 重新获取分享链接 |
| 分享链接已过期 | 超过7天有效期 | 重新创建分享 |
| 登录失败，请稍后重试 | 微信服务器错误 | 稍后重试 |
| 获取微信用户信息失败 | code 无效或已过期 | 重新获取微信授权码 |
| Token无效 | token 过期或被篡改 | 重新登录 |

---

## 十一、前端代码示例

### 11.1 TypeScript 请求封装

```typescript
// request.ts
const BASE_URL = 'https://api.example.com';

interface UnifiedRequest<T> {
  data: T;
  timestamp?: number;
  sign?: string;
}

interface Response<T> {
  code: number;
  message: string;
  data: T;
  timestamp: string;
}

function request<T>(
  url: string,
  data: any,
  token?: string
): Promise<T> {
  return new Promise((resolve, reject) => {
    const header: any = {
      'Content-Type': 'application/json'
    };
    
    if (token) {
      header['Authorization'] = `Bearer ${token}`;
    }

    const requestData: UnifiedRequest<any> = {
      data: data,
      timestamp: Date.now()
    };

    wx.request({
      url: BASE_URL + url,
      method: 'POST',
      header: header,
      data: requestData,
      success: (res) => {
        const response = res.data as Response<T>;
        if (response.code === 200) {
          resolve(response.data);
        } else {
          wx.showToast({
            title: response.message,
            icon: 'none'
          });
          reject(response);
        }
      },
      fail: (err) => {
        wx.showToast({
          title: '网络请求失败',
          icon: 'none'
        });
        reject(err);
      }
    });
  });
}
```

### 11.2 登录流程示例

```typescript
// 登录
async function login() {
  // 1. 获取微信授权码
  const { code } = await wx.login();
  
  // 2. 调用登录接口
  const result = await request<{
    token: string;
    expiresIn: number;
    userInfo: UserInfo;
  }>('/user/login', {
    code: code,
    deviceType: 1
  });
  
  // 3. 保存 token
  wx.setStorageSync('token', result.token);
  wx.setStorageSync('userInfo', result.userInfo);
  
  return result;
}
```

### 11.3 获取账单列表示例

```typescript
// 获取账单列表
async function getRecordList(params: {
  type?: number;
  current?: number;
  size?: number;
}) {
  const token = wx.getStorageSync('token');
  
  const result = await request<Record[]>('/record/list', params, token);
  
  return result;
}
```

---

## 十二、注意事项

1. **Token 处理**
   - 登录成功后务必保存 token
   - 所有需要认证的接口都要在请求头中携带 token
   - token 过期后需要重新登录

2. **时间格式**
   - 日期格式统一使用 `yyyy-MM-dd`
   - 时间戳使用毫秒级时间戳

3. **金额处理**
   - 金额使用 `BigDecimal` 类型，前端使用字符串或数字
   - 涉及金额计算时注意精度问题

4. **分页处理**
   - 默认每页10条
   - 使用分页响应中的 `hasNext` 判断是否还有更多数据

5. **错误处理**
   - 接口返回非200状态码时，显示错误信息
   - 网络请求失败时给出友好提示

6. **数据缓存**
   - 类别列表变化较少，可以本地缓存
   - 账单列表建议每次从服务器获取

7. **分享功能**
   - 分享码有效期为7天
   - 分享链接格式：`/pages/index/index?shareCode=xxx`

---

## 十三、联系支持

如有问题，请联系技术支持。
