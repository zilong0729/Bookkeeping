# 微信记账小程序 - 前端对接指南

## 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v2.0 | 2026-05-23 | 添加管理员接口、健康检查、高并发优化说明 |
| v1.0 | 2026-05-22 | 初始版本 |

---

## 一、接口总体说明

### 1.1 接口规范

- **基础路径**：`https://api.example.com/bookkeeping-server`（生产环境）
- **请求方式**：所有接口统一使用 `POST` 方法
- **数据格式**：统一使用 `JSON` 格式
- **请求头**：
  ```
  Content-Type: application/json
  Authorization: Bearer {token}  // 除登录接口外，其他接口都需要携带Token
  ```

### 1.2 统一请求格式

所有接口的请求参数都需要包装在 `UnifiedRequest` 对象中：

```json
{
  "data": {
    // 具体业务参数
  },
  "timestamp": 1699999999999,  // 可选，时间戳
  "sign": "xxx"  // 可选，签名
}
```

### 1.3 统一响应格式

所有接口的响应数据都遵循以下格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    // 响应数据
  },
  "timestamp": "2026-05-22T10:30:00"
}
```

#### 响应状态码说明：

| 状态码 | 说明 | 处理建议 |
|--------|------|----------|
| 200 | 成功 | 正常处理 |
| 400 | 请求参数错误 | 检查参数格式 |
| 401 | 未授权/Token无效 | 重新登录 |
| 403 | 无权限访问 | 检查用户权限 |
| 404 | 资源不存在 | 检查请求的资源ID |
| 429 | 请求过于频繁 | 稍后重试，建议延迟1-2秒 |
| 500 | 服务器内部错误 | 联系技术支持 |
| 503 | 服务不可用 | 检查系统状态，稍后重试 |

### 1.4 分页响应格式

分页查询接口的 `data` 字段为分页对象：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "list": [
      // 数据列表
    ],
    "total": 100,
    "size": 10,
    "current": 1,
    "pages": 10,
    "hasPrevious": false,
    "hasNext": true
  }
}
```

---

## 二、高并发场景说明

### 2.1 限流机制

系统已启用接口限流，请前端注意以下事项：

#### 限流规则

| 接口类型 | 限流类型 | QPS | 说明 |
|---------|---------|-----|------|
| 创建账单 | USER | 5 | 每秒最多5次 |
| 更新账单 | USER | 10 | 每秒最多10次 |
| 删除账单 | USER | 10 | 每秒最多10次 |
| 查询列表 | USER | 20 | 每秒最多20次 |
| 登录 | IP | 5 | 每IP每秒5次 |

#### 429限流响应

```json
{
  "code": 429,
  "message": "请求过于频繁，请稍后重试",
  "data": null,
  "timestamp": "2026-05-22T10:30:00"
}
```

#### 前端处理建议

```typescript
// 请求重试机制
async function requestWithRetry<T>(
  url: string,
  data: any,
  maxRetries: number = 3
): Promise<T> {
  let lastError;

  for (let i = 0; i < maxRetries; i++) {
    try {
      const result = await request<T>(url, data);
      return result;
    } catch (error: any) {
      lastError = error;

      // 如果是限流错误（429），等待后重试
      if (error.code === 429) {
        const delay = Math.pow(2, i) * 1000; // 指数退避：1s, 2s, 4s
        await new Promise(resolve => setTimeout(resolve, delay));
        continue;
      }

      // 其他错误直接抛出
      throw error;
    }
  }

  throw lastError;
}
```

### 2.2 幂等性处理

#### 防止重复提交

**场景**：用户快速点击、网络延迟导致的重复请求

**解决方案**：
1. 使用防抖/节流
2. 请求前显示loading状态
3. 提交后禁用按钮

```typescript
// 防止重复提交的装饰器示例
function preventDuplicateSubmit(originalFunc: any) {
  let isSubmitting = false;

  return async function(...args: any[]) {
    if (isSubmitting) {
      wx.showToast({ title: '请勿重复提交', icon: 'none' });
      return;
    }

    isSubmitting = true;
    wx.showLoading({ title: '处理中...' });

    try {
      const result = await originalFunc.apply(this, args);
      return result;
    } finally {
      isSubmitting = false;
      wx.hideLoading();
    }
  };
}

// 使用
Page({
  data: {
    isSubmitting: false
  },

  onSubmit: preventDuplicateSubmit(async function() {
    await createRecord(this.data.formData);
    wx.showToast({ title: '创建成功' });
  })
});
```

### 2.3 网络错误处理

```typescript
interface RetryConfig {
  maxRetries: number;
  retryDelay: number;
  retryCondition?: (error: any) => boolean;
}

const defaultRetryConfig: RetryConfig = {
  maxRetries: 3,
  retryDelay: 1000,
  retryCondition: (error) => {
    // 仅在网络错误或限流时重试
    return error.code === 429 ||
           error.code === 503 ||
           error.message?.includes('网络请求失败');
  }
};

async function requestWithRetry<T>(
  url: string,
  data: any,
  config: Partial<RetryConfig> = {}
): Promise<T> {
  const { maxRetries, retryDelay, retryCondition } = {
    ...defaultRetryConfig,
    ...config
  };

  let lastError;

  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      return await request<T>(url, data);
    } catch (error: any) {
      lastError = error;

      // 检查是否应该重试
      if (retryCondition && !retryCondition(error)) {
        throw error;
      }

      // 最后一次尝试失败，不再重试
      if (attempt === maxRetries - 1) {
        break;
      }

      // 指数退避
      const delay = retryDelay * Math.pow(2, attempt);
      await new Promise(resolve => setTimeout(resolve, delay));
    }
  }

  throw lastError;
}
```

---

## 三、接口详细说明

---

### 模块一：用户模块（User）

#### 1. 微信登录

**请求URL**：`POST /user/login`

**接口说明**：用户使用微信授权码登录系统，自动创建用户或返回已存在用户信息。

**请求参数**：
```json
{
  "data": {
    "code": "微信授权code",
    "deviceType": 1,
    "deviceId": "设备唯一标识"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| code | String | ✅ | 微信登录授权码（通过wx.login获取） |
| deviceType | Integer | ❌ | 设备类型：1-小程序，2-Android，3-iOS，默认1 |
| deviceId | String | ❌ | 设备唯一标识 |

**响应示例**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "expiresIn": 604800,
    "userInfo": {
      "id": 1234567890,
      "nickname": "张三",
      "avatarUrl": "https://example.com/avatar.jpg",
      "phone": "13800138000",
      "openId": "oXXXXX",
      "status": 1,
      "role": 0,
      "createTime": "2026-05-01T10:00:00"
    }
  }
}
```

**响应字段说明**：
| 字段 | 类型 | 说明 |
|------|------|------|
| token | String | JWT访问令牌 |
| expiresIn | Long | Token过期时间（秒），默认7天 |
| userInfo | Object | 用户信息对象 |
| userInfo.id | Long | 用户ID |
| userInfo.nickname | String | 用户昵称 |
| userInfo.avatarUrl | String | 头像URL |
| userInfo.phone | String | 手机号（可为空） |
| userInfo.openId | String | 微信OpenID |
| userInfo.status | Integer | 状态：1-正常 |
| userInfo.role | Integer | 角色：0-普通用户，1-管理员 |
| userInfo.createTime | String | 注册时间 |

---

#### 2. 管理员登录

**请求URL**：`POST /admin/login`

**接口说明**：管理员使用用户名密码登录后台管理系统。

**请求参数**：
```json
{
  "data": {
    "username": "admin",
    "password": "your-password"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| username | String | ✅ | 管理员用户名 |
| password | String | ✅ | 登录密码 |

**响应示例**：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "expiresIn": 604800,
    "userInfo": {
      "id": 1,
      "nickname": "系统管理员",
      "role": 1
    }
  }
}
```

**安全说明**：
- 登录失败5次后将锁定15分钟
- 登录成功后会清除失败计数
- 密码使用BCrypt加密存储

---

#### 3. 获取当前用户信息

**请求URL**：`POST /user/info`

**接口说明**：获取当前登录用户的详细信息。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {}
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "id": 1234567890,
    "nickname": "张三",
    "avatarUrl": "https://example.com/avatar.jpg",
    "phone": "13800138000",
    "openId": "oXXXXX",
    "status": 1,
    "role": 0,
    "createTime": "2026-05-01T10:00:00"
  }
}
```

---

#### 4. 更新用户信息

**请求URL**：`POST /user/update`

**接口说明**：更新当前登录用户的个人信息（昵称、头像、手机号）。

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {
    "nickname": "新昵称",
    "avatarUrl": "https://example.com/new-avatar.jpg",
    "phone": "13900139000"
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 1234567890,
    "nickname": "新昵称",
    "avatarUrl": "https://example.com/new-avatar.jpg",
    "phone": "13900139000",
    "openId": "oXXXXX",
    "status": 1,
    "role": 0,
    "createTime": "2026-05-01T10:00:00"
  }
}
```

---

#### 5. 用户登出

**请求URL**：`POST /user/logout`

**请求头**：`Authorization: Bearer {token}`

**请求参数**：
```json
{
  "data": {}
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "登出成功",
  "data": null
}
```

---

## 四、账单模块（Record）

#### 1. 创建账单

**请求URL**：`POST /record`

**接口说明**：创建一条新的账单记录。

**请求头**：`Authorization: Bearer {token}`

**限流说明**：基于用户限流，每秒最多5次请求。

**请求参数**：
```json
{
  "data": {
    "categoryId": 1234567890,
    "type": 1,
    "amount": 100.50,
    "contactId": 9876543210,
    "contactName": "张三",
    "recordDate": "2026-05-22",
    "remark": "购买生活用品"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| categoryId | Long | ✅ | 类别ID |
| type | Integer | ✅ | 类型：1-支出，2-收入 |
| amount | BigDecimal | ✅ | 金额 |
| contactId | Long | ❌ | 联系人ID |
| contactName | String | ❌ | 联系人姓名 |
| recordDate | String | ❌ | 记账日期，默认当天 |
| remark | String | ❌ | 备注 |

**响应示例**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 111222333,
    "categoryId": 1234567890,
    "categoryName": "餐饮",
    "categoryIcon": "🍜",
    "type": 1,
    "amount": 100.50,
    "contactId": 9876543210,
    "contactName": "张三",
    "recordDate": "2026-05-22",
    "remark": "购买生活用品",
    "createTime": "2026-05-22T10:30:00",
    "updateTime": "2026-05-22T10:30:00"
  }
}
```

---

#### 2. 更新账单

**请求URL**：`POST /record/update`

**限流说明**：每秒最多10次请求。

**请求参数**：
```json
{
  "data": {
    "id": 111222333,
    "categoryId": 1234567890,
    "type": 1,
    "amount": 150.00,
    "contactId": 9876543210,
    "contactName": "张三",
    "recordDate": "2026-05-22",
    "remark": "更新备注"
  }
}
```

---

#### 3. 删除账单

**请求URL**：`POST /record/delete`

**请求参数**：
```json
{
  "data": {
    "id": 111222333
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 4. 获取账单详情

**请求URL**：`POST /record/detail`

**请求参数**：
```json
{
  "data": {
    "id": 111222333
  }
}
```

---

#### 5. 获取账单列表

**请求URL**：`POST /record/list`

**限流说明**：每秒最多20次请求。

**请求参数**：
```json
{
  "data": {
    "type": 1,
    "categoryId": 1234567890,
    "contactName": "张三",
    "startDate": "2026-05-01",
    "endDate": "2026-05-31",
    "current": 1,
    "size": 10
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | Integer | ❌ | 类型：1-支出，2-收入 |
| categoryId | Long | ❌ | 类别ID |
| contactName | String | ❌ | 联系人姓名（模糊查询） |
| startDate | String | ❌ | 开始日期（yyyy-MM-dd） |
| endDate | String | ❌ | 结束日期（yyyy-MM-dd） |
| current | Long | ❌ | 当前页码，默认1 |
| size | Long | ❌ | 每页条数，默认10 |

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 111222333,
      "categoryId": 1234567890,
      "categoryName": "餐饮",
      "categoryIcon": "🍜",
      "type": 1,
      "amount": 150.00,
      "contactId": 9876543210,
      "contactName": "张三",
      "recordDate": "2026-05-22",
      "remark": "午餐",
      "createTime": "2026-05-22T10:30:00",
      "updateTime": "2026-05-22T11:00:00"
    }
  ]
}
```

---

#### 6. 获取账单统计

**请求URL**：`POST /record/statistics`

**限流说明**：每秒最多30次请求。

**请求参数**：
```json
{
  "data": {
    "startDate": "2026-05-01",
    "endDate": "2026-05-31"
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "totalIncome": 5000.00,
    "totalExpense": 3500.00,
    "balance": 1500.00,
    "incomeCount": 2,
    "expenseCount": 15
  }
}
```

---

## 五、类别模块（Category）

#### 1. 获取类别列表

**请求URL**：`POST /category/list`

**缓存建议**：类别列表变化较少，建议本地缓存，定时刷新。

**请求参数**：
```json
{
  "data": {
    "type": 1
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| type | Integer | ❌ | 类型：1-支出，2-收入，不传则返回全部 |

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {
      "id": 1,
      "name": "餐饮",
      "type": 1,
      "icon": "🍜",
      "sort": 1,
      "createTime": "2026-01-01T00:00:00"
    }
  ]
}
```

---

#### 2-4. 创建/更新/删除类别

**创建URL**：`POST /category/create`
**更新URL**：`POST /category/update`
**删除URL**：`POST /category/delete`

具体参数和响应格式参见 [原文档](#六、类别模块（Category）) 相关章节。

---

## 六、联系人模块（Contact）

#### 1-5. 创建/更新/删除/详情/列表

具体接口说明参见 [原文档](#七、联系人模块（Contact）) 相关章节。

---

## 七、我的事件模块（MyEvent）

#### 1-5. 创建/更新/删除/详情/列表

具体接口说明参见 [原文档](#八、我的事件模块（MyEvent）) 相关章节。

---

## 八、分享模块（Share）

#### 1. 创建分享邀请

**请求URL**：`POST /share/create`

**接口说明**：创建分享链接，可以是事件分享或好友邀请。

**并发控制**：系统使用分布式锁保证分享码唯一性。

**请求参数**：
```json
{
  "data": {
    "shareType": 1,
    "relatedId": 300,
    "relation": "friend"
  }
}
```

**参数说明**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| shareType | Integer | ✅ | 分享类型：1-事件分享、2-账单分享、3-邀请注册 |
| relatedId | Long | ❌ | 关联ID（事件ID或账单ID） |
| relation | String | ❌ | 关系类型：friend/family/colleague/other |

**响应示例**：
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 400,
    "shareCode": "abc123def456",
    "shareUrl": "/pages/index/index?shareCode=abc123def456",
    "shareType": 1,
    "status": 0,
    "expireTime": "2026-05-29T15:00:00",
    "shareNickname": "张三",
    "shareAvatar": "https://example.com/avatar.jpg",
    "event": {
      "id": 300,
      "title": "我的婚礼",
      "eventType": 1,
      "eventDate": "2026-06-01",
      "location": "北京市朝阳区某某酒店"
    },
    "createTime": "2026-05-22T15:00:00"
  }
}
```

---

#### 2. 获取分享信息

**请求URL**：`POST /share/info`

**接口说明**：通过分享码获取分享信息（无需登录）。

**请求参数**：
```json
{
  "data": {
    "shareCode": "abc123def456"
  }
}
```

---

#### 3. 接受分享邀请

**请求URL**：`POST /share/accept`

**接口说明**：接受分享邀请，建立好友关系（需要登录）。

**并发控制**：同一用户不能同时接受同一分享链接，防止重复创建好友关系。

**请求参数**：
```json
{
  "data": {
    "shareCode": "abc123def456"
  }
}
```

**响应示例**：
```json
{
  "code": 200,
  "message": "接受成功",
  "data": null
}
```

---

## 九、提醒任务模块（Reminder）

#### 1-7. 创建/更新/删除/详情/列表/取消/分享参数

具体接口说明参见 [原文档](#十、提醒任务模块（Reminder）) 相关章节。

---

## 十、系统监控模块（Monitor）

### 1. 健康检查

**请求URL**：`GET /monitor/health`

**接口说明**：检查系统各组件健康状态，无需登录认证。

**应用场景**：
- 启动时检查后端服务是否可用
- 定时检测系统健康状态
- 告警系统健康检查

**响应示例**：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "status": "UP",
    "database": {
      "status": "UP",
      "activeCount": 15,
      "poolingCount": 25,
      "maxActive": 200,
      "message": "数据库连接正常"
    },
    "redis": {
      "status": "UP",
      "message": "Redis连接正常"
    },
    "threadPool": {
      "status": "UP",
      "availableProcessors": 8,
      "maxMemory": "4.00 GB",
      "totalMemory": "2.00 GB",
      "freeMemory": "0.50 GB",
      "usedMemory": "1.50 GB",
      "memoryUsagePercent": "37.50"
    },
    "timestamp": 1747987200000
  }
}
```

**前端使用示例**：

```typescript
// 检查系统健康状态
async function checkHealth(): Promise<boolean> {
  try {
    const response = await wx.request({
      url: `${BASE_URL}/monitor/health`,
      method: 'GET',
      header: {
        'Content-Type': 'application/json'
      }
    });

    const result = response.data as Response<any>;
    return result.code === 200 && result.data.status === 'UP';
  } catch (error) {
    console.error('健康检查失败', error);
    return false;
  }
}

// 应用启动时检查
async function onAppLaunch() {
  const isHealthy = await checkHealth();

  if (!isHealthy) {
    wx.showModal({
      title: '服务不可用',
      content: '后端服务暂时不可用，请稍后再试',
      showCancel: false
    });
  }
}
```

---

## 十一、错误码说明

### 11.1 通用错误码

| 错误信息 | 错误码 | 可能原因 | 解决方案 |
|----------|--------|----------|----------|
| 操作成功 | 200 | - | - |
| 请求参数错误 | 400 | 参数格式错误 | 检查请求参数 |
| 未授权 | 401 | Token无效或过期 | 重新登录 |
| 无权限访问 | 403 | 权限不足 | 检查用户角色 |
| 资源不存在 | 404 | ID错误或已删除 | 检查资源ID |
| 请求过于频繁 | 429 | 触发限流 | 稍后重试，使用指数退避 |
| 系统繁忙，请稍后重试 | 500 | 服务器内部错误 | 联系技术支持 |
| 服务不可用 | 503 | 系统维护或过载 | 稍后重试 |

### 11.2 业务错误码

| 错误信息 | 可能原因 | 解决方案 |
|----------|----------|----------|
| 类别不存在 | categoryId错误 | 检查categoryId是否正确 |
| 类别类型不匹配 | 支出类别用于收入 | 确保type与categoryId对应 |
| 账单不存在 | id错误或已删除 | 检查id是否正确 |
| 无权修改该账单 | 非账单所有者 | 确保是账单创建者 |
| 联系人不存在 | id错误或已删除 | 检查id是否正确 |
| 分享链接不存在 | shareCode错误或已过期 | 重新获取分享链接 |
| 分享链接已过期 | 超过7天有效期 | 重新创建分享 |
| 登录失败，请稍后重试 | 微信服务器错误 | 稍后重试 |
| 获取微信用户信息失败 | code无效或已过期 | 重新获取微信授权码 |
| Token无效 | token过期或被篡改 | 重新登录 |
| 账号已被禁用 | 用户被管理员禁用 | 联系管理员 |
| 操作进行中，请稍后重试 | 并发操作冲突 | 稍后重试 |
| 登录失败次数过多 | 连续登录失败5次 | 15分钟后再试 |
| 用户不存在 | 用户ID错误或已删除 | 检查用户ID |

---

## 十二、接口调用顺序

### 12.1 用户登录流程

```
1. POST /user/login
   ↓
   获取 token 和 userInfo
   ↓
   保存 token 到本地存储
   ↓
2. POST /user/info（获取完整用户信息，可选）
```

### 12.2 首页数据加载流程

```
1. POST /category/list（获取账单类别）
   ↓
2. POST /record/statistics（获取本月统计）
   ↓
3. POST /record/list（获取最近账单）
   ↓
4. POST /my-event/list（获取即将到来的事件）
```

### 12.3 添加账单流程

```
1. POST /category/list（选择类别）
   ↓
2. POST /contact/list（选择联系人，可选）
   ↓
3. POST /record（创建账单）
   ↓
   （如果返回429，使用指数退避重试）
```

### 12.4 分享好友流程

```
1. POST /share/create（创建分享）
   ↓
2. 获取 shareCode，生成小程序路径
   ↓
3. 调用 wx.showShareMenu 分享
```

### 12.5 好友接受分享流程

```
1. 好友打开小程序，通过 onLoad 获取 shareCode
   ↓
2. POST /share/info（获取分享信息）
   ↓
3. 如果未登录：
   POST /user/login（微信登录）
   ↓
4. POST /share/accept（接受分享）
   ↓
   （系统自动创建好友关系）
```

---

## 十三、前端代码示例

### 13.1 TypeScript 请求封装（增强版）

```typescript
// request.ts - 增强版请求封装

interface UnifiedRequest<T> {
  data: T;
  timestamp?: number;
  sign?: string;
}

interface Response<T> {
  code: number;
  message: string;
  data: T;
  timestamp: string;
}

interface RetryConfig {
  maxRetries: number;
  retryDelay: number;
  retryStatusCodes: number[];
}

const BASE_URL = 'https://api.example.com/bookkeeping-server';
const defaultRetryConfig: RetryConfig = {
  maxRetries: 3,
  retryDelay: 1000,
  retryStatusCodes: [429, 503]
};

class RequestManager {
  private token: string = '';
  private retryConfig: RetryConfig;

  constructor(config: Partial<RetryConfig> = {}) {
    this.retryConfig = { ...defaultRetryConfig, ...config };
    this.token = wx.getStorageSync('token') || '';
  }

  setToken(token: string) {
    this.token = token;
    wx.setStorageSync('token', token);
  }

  clearToken() {
    this.token = '';
    wx.removeStorageSync('token');
  }

  async request<T>(
    url: string,
    data: any = {},
    options: {
      method?: 'GET' | 'POST';
      requireAuth?: boolean;
      retry?: boolean;
    } = {}
  ): Promise<T> {
    const {
      method = 'POST',
      requireAuth = true,
      retry = true
    } = options;

    const header: Record<string, string> = {
      'Content-Type': 'application/json'
    };

    if (requireAuth && this.token) {
      header['Authorization'] = `Bearer ${this.token}`;
    }

    const requestData: UnifiedRequest<any> = {
      data,
      timestamp: Date.now()
    };

    const request = async (attempt: number = 0): Promise<T> => {
      try {
        const response = await wx.request({
          url: BASE_URL + url,
          method,
          header,
          data: requestData
        });

        const result = response.data as Response<T>;

        // 处理业务错误
        if (result.code !== 200) {
          // 401 未授权，清除token
          if (result.code === 401) {
            this.clearToken();
            wx.navigateTo({ url: '/pages/login/login' });
          }

          // 429 限流，需要重试
          if (result.code === 429 && retry && attempt < this.retryConfig.maxRetries) {
            const delay = this.retryConfig.retryDelay * Math.pow(2, attempt);
            await this.sleep(delay);
            return request(attempt + 1);
          }

          throw new Error(result.message);
        }

        return result.data;
      } catch (error: any) {
        // 网络错误，重试
        if (retry &&
            attempt < this.retryConfig.maxRetries &&
            (error.message?.includes('网络') || error.code === 503)) {
          const delay = this.retryConfig.retryDelay * Math.pow(2, attempt);
          await this.sleep(delay);
          return request(attempt + 1);
        }

        throw error;
      }
    };

    return request();
  }

  private sleep(ms: number): Promise<void> {
    return new Promise(resolve => setTimeout(resolve, ms));
  }
}

export const requestManager = new RequestManager();
```

### 13.2 API 模块封装

```typescript
// api/user.ts
export const UserAPI = {
  login: (code: string, deviceType: number = 1, deviceId?: string) => {
    return requestManager.request('/user/login', {
      code,
      deviceType,
      deviceId
    });
  },

  getInfo: () => {
    return requestManager.request('/user/info', {});
  },

  update: (data: { nickname?: string; avatarUrl?: string; phone?: string }) => {
    return requestManager.request('/user/update', data);
  },

  logout: () => {
    return requestManager.request('/user/logout', {});
  }
};

// api/record.ts
export const RecordAPI = {
  create: (data: {
    categoryId: number;
    type: number;
    amount: number;
    recordDate?: string;
    remark?: string;
  }) => {
    return requestManager.request('/record', data);
  },

  update: (data: any) => {
    return requestManager.request('/record/update', data);
  },

  delete: (id: number) => {
    return requestManager.request('/record/delete', { id });
  },

  detail: (id: number) => {
    return requestManager.request('/record/detail', { id });
  },

  list: (params: {
    type?: number;
    categoryId?: number;
    startDate?: string;
    endDate?: string;
    current?: number;
    size?: number;
  }) => {
    return requestManager.request('/record/list', params);
  },

  statistics: (params: { startDate?: string; endDate?: string }) => {
    return requestManager.request('/record/statistics', params);
  }
};

// api/share.ts
export const ShareAPI = {
  create: (data: {
    shareType: number;
    relatedId?: number;
    relation?: string;
  }) => {
    return requestManager.request('/share/create', data);
  },

  info: (shareCode: string) => {
    return requestManager.request('/share/info', { shareCode });
  },

  accept: (shareCode: string) => {
    return requestManager.request('/share/accept', { shareCode });
  }
};
```

### 13.3 登录流程示例

```typescript
// pages/login/login.ts

Page({
  data: {
    isLoading: false
  },

  async onLogin() {
    if (this.data.isLoading) return;

    this.setData({ isLoading: true });

    try {
      // 1. 获取微信授权码
      const { code } = await wx.login();

      // 2. 调用登录接口（带重试）
      const result = await UserAPI.login(code, 1);

      // 3. 保存 token
      requestManager.setToken(result.token);
      wx.setStorageSync('userInfo', result.userInfo);

      // 4. 跳转首页
      wx.switchTab({ url: '/pages/index/index' });

    } catch (error: any) {
      wx.showToast({
        title: error.message || '登录失败',
        icon: 'none'
      });
    } finally {
      this.setData({ isLoading: false });
    }
  }
});
```

### 13.4 分享功能示例

```typescript
// pages/event/event.ts

Page({
  data: {
    event: null as any,
    shareCode: ''
  },

  async onLoad(options: any) {
    // 获取分享码
    if (options.shareCode) {
      this.data.shareCode = options.shareCode;
      await this.loadShareInfo();
    }
  },

  async loadShareInfo() {
    try {
      const shareInfo = await ShareAPI.info(this.data.shareCode);
      this.setData({ event: shareInfo.event });
    } catch (error) {
      wx.showToast({
        title: '分享信息加载失败',
        icon: 'none'
      });
    }
  },

  async onAcceptShare() {
    try {
      await ShareAPI.accept(this.data.shareCode);
      wx.showToast({
        title: '已添加为好友',
        icon: 'success'
      });
    } catch (error: any) {
      wx.showToast({
        title: error.message || '接受失败',
        icon: 'none'
      });
    }
  },

  // 分享配置
  onShareAppMessage() {
    const event = this.data.event;
    return {
      title: event?.title || '邀请您参加活动',
      path: `/pages/event/event?shareCode=${this.data.shareCode}`,
      imageUrl: event?.imageUrl
    };
  }
});
```

### 13.5 防抖与节流

```typescript
// utils/throttle.ts

// 防抖函数
export function debounce<T extends (...args: any[]) => any>(
  func: T,
  wait: number = 500
): (...args: Parameters<T>) => void {
  let timeout: number | null = null;

  return function(this: any, ...args: Parameters<T>) {
    if (timeout !== null) {
      clearTimeout(timeout);
    }

    timeout = setTimeout(() => {
      func.apply(this, args);
      timeout = null;
    }, wait);
  };
}

// 节流函数
export function throttle<T extends (...args: any[]) => any>(
  func: T,
  wait: number = 1000
): (...args: Parameters<T>) => void {
  let lastTime: number | null = null;

  return function(this: any, ...args: Parameters<T>) {
    const now = Date.now();

    if (lastTime === null || now - lastTime >= wait) {
      lastTime = now;
      func.apply(this, args);
    }
  };
}

// 使用示例
Page({
  data: {
    searchKey: ''
  },

  onSearch: throttle(async function(this: any, e: any) {
    const keyword = e.detail.value;
    const result = await RecordAPI.list({ keyword });
    this.setData({ records: result });
  }, 1000)
});
```

---

## 十四、注意事项

### 14.1 Token 处理
- 登录成功后务必保存 token
- 所有需要认证的接口都要在请求头中携带 token
- token 过期后需要重新登录（401错误）
- 建议在 app.js 中检查 token 有效性

### 14.2 时间格式
- 日期格式统一使用 `yyyy-MM-dd`
- 时间戳使用毫秒级时间戳
- 返回的时间字符串格式为 `yyyy-MM-ddTHH:mm:ss`

### 14.3 金额处理
- 金额使用 `BigDecimal` 类型，前端使用字符串或数字
- 涉及金额计算时注意精度问题，建议使用整数运算（分）
- 显示时注意千分位格式化

### 14.4 分页处理
- 默认每页10条
- 使用分页响应中的 `hasNext` 判断是否还有更多数据
- 下拉刷新时重置分页

### 14.5 错误处理
- 接口返回非200状态码时，显示错误信息
- 网络请求失败时给出友好提示
- 429错误使用指数退避重试
- 503错误提示用户稍后再试

### 14.6 数据缓存
- 类别列表变化较少，可以本地缓存，定时刷新
- 账单列表建议每次从服务器获取
- 用户信息缓存在本地，登录后更新

### 14.7 分享功能
- 分享码有效期为7天
- 分享链接格式：`/pages/index/index?shareCode=xxx`
- 接受分享时会自动建立好友关系

### 14.8 高并发场景
- 快速操作时注意防止重复提交
- 使用防抖/节流限制请求频率
- 网络不稳定时实现自动重试
- 429限流时使用指数退避策略

### 14.9 用户状态
- 如果用户被禁用（status=0），需要提示用户联系管理员
- 管理员登录和普通用户登录使用不同接口

---

## 十五、环境配置

### 15.1 开发环境
```
API_BASE_URL: http://localhost:8080/bookkeeping-server
```

### 15.2 测试环境
```
API_BASE_URL: https://test-api.example.com/bookkeeping-server
```

### 15.3 生产环境
```
API_BASE_URL: https://api.example.com/bookkeeping-server
```

---

## 十六、联系支持

如有问题，请通过以下方式联系技术支持：

- 邮箱：support@example.com
- 技术文档：https://docs.example.com
- 问题反馈：https://github.com/example/bookkeeping/issues

---

**最后更新**：2026-05-23
**版本**：v2.0
